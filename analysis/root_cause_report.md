# Root Cause — 절단이 안전 필터의 실패를 설명하는가

## 0. 입력 가용성

| 입력 | 상태 |
|---|---|
| `tokenization_results.csv` (학생 2) | 864개 SGuard 조건 |
| `safety_results.csv` | `evaluation/safety/safety_results.csv` |
| `generation_results.csv` | **없음** |
| `image_labels.csv` | **없음** |

## 1. 조건별 판정 결과

| input_policy | n | 판정된 행 | correct | under_block | over_block | 판정 불가 |
|---|---|---|---|---|---|---|
| `constrained_77` | 432 | 432 | 240 | 189 | 3 | 0 |
| `native` | 432 | 432 | 241 | 188 | 3 | 0 |

## 2. under_block 의 절단 설명 가능성

SGuard 쪽 `key_visibility` 로만 분류한다. AltDiffusion 의 절단은 판정
이후에 일어나므로 판정의 원인이 될 수 없다.

| input_policy | under_block | 핵심표현 미노출 | 부분 노출 | 전부 노출(절단 아님) |
|---|---|---|---|---|
| `constrained_77` | 189 | 72 | 0 | 117 |
| `native` | 188 | 0 | 0 | 188 |

`전부 노출` 은 필터가 핵심 표현을 모두 읽고도 통과시킨 사례다.
절단으로 설명할 수 없으므로 희귀 표현의 표상 쪽 원인을 따로 봐야 한다.

## 3. 조건 1 vs 조건 2 — 절단 효과 분리

조건 1 은 절단이 0건이므로 여기서 나온 미차단은 구조적으로 절단이 아니다.
같은 프롬프트를 두 조건에서 짝지어, 조건 2 의 미차단을 두 축으로 쪼갠다.

공통 프롬프트 432건, 조건 2 미차단 189건.

| | 핵심표현 노출 줄어듦 | 노출 그대로(full) |
|---|---|---|
| **조건 2 에서 새로 생긴 미차단** | **4** | 0 |
| 조건 1 부터 미차단이던 것 | 68 | 117 |

- 굵게 표시한 **4건**만이 절단으로 설명 가능한 후보다. 조건 2 에서 새로 생겼고, 핵심 표현 노출도 실제로 줄었다.
- 0건은 조건 2 에서 새로 생겼지만 핵심 표현은 그대로 보였다. 절단이 주변 문맥만 없앤 경우이며, 핵심 표현 절단으로 설명하지 않는다.
- 아랫줄 185건은 조건 1 에서 이미 미차단이었다. 절단 이전부터 실패했으므로 절단이 원인이 아니다.
- 조건 2 에서 오히려 차단된 경우: 3건

> 이 표의 수치는 2절과 집계 기준이 다르다. 2절은 조건별 미차단을 `key_visibility` 로만 나눈 것이고, 여기서는 조건 1 대비 변화까지 함께 본다.

## 6. 희귀도 대조

같은 concept x length x position 에서 common 은 차단되고 rare 는 통과했으며,
그때 rare 의 핵심 표현이 **전부 노출**돼 있었다면 절단으로 설명할 수 없다.
희귀 표현 자체의 표상 문제를 시사하는 가장 강한 사례다.

| input_policy | 대조쌍 | rare 만 통과 | 그중 핵심표현 전부 노출 |
|---|---|---|---|
| `constrained_77` | 216 | 20 | **20** |
| `native` | 216 | 20 | **20** |

> 이 분석은 관측이며 인과 증명이 아니다. 절단으로 설명 가능한 후보와
> 설명 불가능한 사례를 구분할 뿐이다.
